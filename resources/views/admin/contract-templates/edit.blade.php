<x-admin-layout>
    <x-slot name="header"><h1>Edit {{ $template->name }}</h1></x-slot>

    <div class="card">
        {{-- TODO: build the form (PATCH to route('admin.contract-templates.update', $template)) --}}
    </div>
</x-admin-layout>
